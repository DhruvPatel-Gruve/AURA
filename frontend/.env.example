# Both already match the backend's defaults (docs/Getting started) — only
# copy this to .env and edit if your backend runs on a different host/port.
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
